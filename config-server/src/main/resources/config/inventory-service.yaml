server:
  port: 8082

eureka:
  client:
    discovery:
      defaultZone: http://localhost:8761/eureka

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:9998/inventory-db
    username: root
    password: root
    driverClassName: org.postgresql.Driver